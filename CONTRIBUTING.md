# Contributing to Mana Drain

Thank you for your interest in contributing to Mana Drain! 🎮

## 🚀 Getting Started

1. Fork the repository
2. Clone your fork: `git clone https://github.com/YOUR_USERNAME/mana-drain.git`
3. Create a branch: `git checkout -b feature/your-feature-name`
4. Install dependencies: `npm install`
5. Make your changes
6. Test your changes: `npm run build && npm run lint`
7. Commit your changes: `git commit -m 'Add some feature'`
8. Push to your branch: `git push origin feature/your-feature-name`
9. Open a Pull Request

## 📋 Development Guidelines

### Code Style

- Use TypeScript strictly (no `any` types unless absolutely necessary)
- Follow the existing code structure and patterns
- Use functional components with hooks
- Prefer named exports over default exports
- Use Tailwind CSS for all styling (no inline styles)
- Use Lucide React for all icons

### Component Guidelines

- All components should be in TypeScript
- Use Framer Motion for animations
- Follow the glassmorphic design system
- Ensure mobile-first responsive design
- Add proper TypeScript types for props

### Commit Messages

We follow conventional commits:

- `feat:` New feature
- `fix:` Bug fix
- `docs:` Documentation changes
- `style:` Code style changes (formatting, etc.)
- `refactor:` Code refactoring
- `test:` Adding or updating tests
- `chore:` Maintenance tasks

### Pull Request Process

1. Ensure your code passes all linting checks
2. Update documentation if needed
3. Add tests if applicable
4. Ensure the build succeeds
5. Fill out the PR template completely
6. Request review from maintainers

## 🐛 Reporting Bugs

Use the [Bug Report template](.github/ISSUE_TEMPLATE/bug_report.md) when reporting bugs.

## 💡 Suggesting Features

Use the [Feature Request template](.github/ISSUE_TEMPLATE/feature_request.md) for new feature suggestions.

## ❓ Questions?

Open a discussion or check existing issues/PRs for answers.

## 📝 License

By contributing, you agree that your contributions will be licensed under the MIT License.

